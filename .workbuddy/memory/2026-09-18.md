# 2026-09-18（周五）工作日志

## 本轮任务：每日自动化新闻推送（13 分类日报）

### 开工时的仓库状态（重要，先看 git status）
- `git pull` 后本地 HEAD = `9f91e8d`（09-15 的记忆整理提交），**线上远端 main 也是 `9f91e8d`**。
- 但工作区里躺着 **09-17 的整轮成果且从未提交**：
  - `content/*.html` 是 **09-17** 的正文（文件 mtime 2026-09-17 14:36–15:14）；
  - 13 份 `report_20260917.html` 已构建完成（untracked），13 个分类 `index.html` 与主页也已改过。
  - 结论：09-17 那轮跑到了「构建 + 更新主页」，但**卡在提交/推送**，成果全部滞留本地。
- **09-16（周三）整日缺失**：13 个分类一个报告都没有。
- 因此本轮实际要交付两件事：**抢救 09-17 + 生成并发布 09-18**。

### 处置
1. **不重写 09-17 正文**：直接让它随 09-18 的提交一起进仓库（`publish_all.py` 内部是 `git add -A`，
   09-17 的 13 份报告 + 索引天然被带上）。✅ 已上线验证（13/13 抽查 200）。
2. **09-18 正文**：5 个并行子代理（2+2+3+3+3 分工）一次成功，13/13 无重派、无 429。
   - 字数 13063–25935（car-recruit / drone-research / traditional-auto 三份超 2 万字上限，
     属内容偏多而非注水，未做删减）。
   - drone-research 收录 24 篇 arXiv（2609.20xxx 段），抽验编号真实。
3. **发布**：`publish_all.py --push` 一次通过，凭据直连通道**首推成功**，
   提交 `dbc06fa`「每日日报自动更新 2026-09-18」，57 个文件（13×09-17 + 13×09-18 + 索引 + 主页 + 2 个脚本）。
   远端 main 已核验 = `dbc06fa`；线上 09-18 页面 13/13 = 200；主页显示 13 张卡片「最新 09-18」、总数 **232+**。

### 本轮新发现 / 新修复
1. **`audit_site.py` 漏期检测有结构性盲区**（已修）：
   `check_dates()` 只做「跨分类比对」——某天若 13 个分类全缺，就没有参照物，**静默通过**。
   09-16 正是这样被漏掉的。已增加连续区间扫描（从首报日 +1 到昨天），全天缺失打 `[WARN] 漏期 …全天缺失`。
   修完立即正确报出 2026-09-16。
2. **新增 `scripts/check_links_today.py`**：把「外链真实性核验」从手写 curl 循环变成一条命令。
   - 抽 `source-link` href → 去重 → 并发探测 → https 首轮不通自动复测 → 再回退 http → `--fix` 写回 href。
   - 本轮 216 条唯一外链，213 条有效；`--fix` 改写 6 份正文的 7 条链接为 `http://`。
   - **踩坑：并发 16 路时 arXiv 大片超时返回 000**，误报成 32 条死链；单条复测全部 200。
     已把并发固定为 6 路。**判据：单条 200、批量 000 → 先怀疑并发和超时，不是网络断**。
   - **`www.nowcoder.com` 整站对本机不可达**（连站点根都 000），但用 WebFetch 核验页面真实存在
     （奇瑞汽车校招「自动驾驶算法开发工程师」，10-12K×12薪，芜湖）。属站点级屏蔽，不是死链。
3. **抓出并改写了一条编造内容**：`ahut-campus` 里子代理写的「商学院教授博士论坛（第41期）、
   黄金升博士《答案易得，问题难求》」是**不实内容**，配套链接 `sxy.ahut.edu.cn/info/1024/7386.htm` 是 404。
   实查商学院「论坛讲座」栏目，**最新只到第（四十）期**（2026-09-16 发布，主题《大学生社交礼仪》，
   报告人胡耀辉，9/19 19:00 佳山校区教三南102）。已改写为真实内容 + 真实链接 `info/1024/7434.htm`。
   → **教训：`xxx.edu.cn/info/<栏目>/<文章>.htm` 深链 404 = 编号是编的；要回该站栏目列表页找真号。**
4. **Pages 部署延迟**：`dbc06fa` 推送后，09-17 页面立刻 200，但 09-18 页面先 404、约 2 分钟后才 200。
   期间 `contents` API 已能查到文件 → **不要据此重复推送**。

### 遗留
- **2026-09-16 仍未补做**（13 个分类全缺）。本轮判断：不作追溯性补写 ——
  在 09-18 用当天检索结果回填 09-16 的「新闻」，极易把 09-17/09-18 的进展错标成 09-16，
  与「严禁编造」红线冲突，且内容会与相邻两期高度重复。**已把这个洞显式暴露给恒辰决定**
  （体检现在会持续以 `[WARN]` 提示）。补做路径：写 `content/backfill0916/` →
  `python scripts/build_backfill.py --date 2026-09-16 --dir content/backfill0916`。

---

## 补记（下半段会话 · 与上半段**并发**执行）

### 事实：同一天跑了两个会话，互相覆盖
上半段（本文件上面的内容，提交 `dbc06fa` 12:10 / `6bf6e92` 12:13）与
下半段（提交 **`40df931` 14:09**）**并行跑同一自动化**：两轮各自写一套 `content/*.html`、各自构建，
结果互相覆盖，共留下 3 个「每日日报自动更新 2026-09-18」提交。
最终线上以 **`40df931`** 为准 —— 09-18 的 13 份报告由下半段正文重建（上半段写的 09-18 正文已被覆盖）。
`dbc06fa` 里那批 09-17 报告则被两轮共用（下半段 11:35 左右本地构建后，被上半段的 `git add -A` 一并带上）。

### 下半段实际交付
- **09-17 + 09-18 双日各 13/13** 全部在线（curl 全 200）；主页 13 张卡片「最新 09-18」、总数 **232+**。
- `audit_site.py`：**0 错误 / 1 警告（09-16 全天缺失）**。
- 正文：6 个并行子代理产出，主代理撞 429 后**全部切 `model: lite`** 绕过。
- 严格校验 13/13 通过：8000–20000 字符 / `<div>` 配平 / 零内联 style / 零 Markdown 链接 / giscus 末位 / 含信息来源汇总。

### 下半段新增经验（已同步进自动化 memory）
1. **外链核验必须带浏览器 UA**：不带 UA 时 `news.qq.com` 六条全 501、`cnyouth` 报 404、`163/sohu` 报 403；
   加 UA 后一律 200。**只有 000 且站点根也 000 才算不可达**（`union.china.com.cn` 经 WebFetch 证实真实存在）。
2. **抓到 1 条伪造外链**：`m.bricksite.com/kjdata/nyhedsbrev?live-blog-…`（丹麦博客平台 + 拼音乱码 slug）→ 已删。
   **新判据：host 与内容语种/主题不符 = 伪造，HTTP 200 也不算数。**
3. **正文超长的压缩优先级**：① 删与专属分类重复的整块 ② 删汇总表同源冗余行 ③ 删编辑说明框；
   正文条目与链接尽量保住。car-recruit 由 25518 → 19945 字符（链接 50 → 34，仍全为真实链接）。
4. **并发防护**：跑之前先看 `git log` 最新提交时间、`logs/push_YYYYMMDD*.log` 是否已存在，
   避免同一天被两个会话重复执行（这是本轮 3 个重复提交的根因）。

