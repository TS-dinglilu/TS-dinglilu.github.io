# 自动化执行记录 — 每日自动化新闻推送（13 分类日报）

> 站点：https://ts-dinglilu.github.io/ ｜ 仓库：`D:\研二\github.auto\repo`（main 直推）
> 仅记录高层结果，正文内容不入档。

## 2026-09-14（周一）
- **实际执行日 09-14**：本会话在 09-13 启动、跨天到 09-14 被恢复执行。
- **核实历史**：09-12、09-13 已由中断前的会话完成并推送（commit `695a0c0` / `0697ac7`），本轮仅核实，未重做。
- **本轮产出**：13 分类 09-14 正文（content/<分类>.html，逐条真实链接），构建 `report_20260914.html` ×13 + 13 个归档索引 + 主页统计。
- **推送结果**：09-14 报告（`1fb5aab`）与主页修复（`15f50f2`）**已上线并验证**（各分类 09-14 页面 HTTP 200，主页显示 13 张卡片「最新 09-14」、总数 180+）。
  文档提交 `9a35044` 一度因 `github.com:443` 中断暂留本地；**当晚重试凭据直连通道第 3 次成功，`9a35044` 已推送**，
  远端 main = `9a35044`，13 个 09-14 页面 curl 全 200（无需再带出）。
- **关键修复（重要）**：主页 index.html 被设计编辑器改写，元素被批量加上 `data-page-node-id` 属性，
  导致 `update_homepage.py` 三处精确字符串匹配失效（`<div class="stat-num">` / `auto-meta` / `auto-updated`），
  于是 **09-12、09-13、09-14 连续三次运行主页其实都没真正更新**（卡片停在 09-11、总数停在 141+）。
  已改为「前缀 + 任意属性」正则并保留元素原有属性，重跑后恢复（13 张卡片刷新、总数 180+）。
- **环境坑**：① Bash PATH 偶发损坏（`dirname`/`ls`/`git` 全不识别）→ 命令前 `export PATH` 指向 PortableGit 的 `usr/bin` 与 `mingw64/bin`；② 子代理 429 限流 → 换 `model: "lite"` 立即绕过。
- **仍缺日期**：09-10（`content/backfill0910/` 草稿仅 11/13，缺 byd-recruit、traditional-auto），距今 4 天，本轮未补。

### 同日续跑补充（第二轮，09-14 下午）
- **核实结果**：09-12 / 09-13 / 09-14 三天各 13/13 报告齐备，远端 main = `9a35044`，本轮无漏跑告警。
- **正文产出核对**：本轮另派 5 个并行子代理写 13 分类正文（3+2+3+3+2 分工）。
  **其中研究「research-institute + future-planning」的代理零产出**，已重派；
  `future-planning` 重派时撞 429，换 `model: "lite"` 成功。
  ⚠️ **教训：每轮写完必须逐个核对文件 mtime/大小，不能只信代理的回报文字。**
- **本轮新增踩坑**：
  1. **`git status` 显示 "ahead 15 commits"、`git rev-parse origin/main` 停在很旧的 `cdb3b4c`，但远端其实已收到推送**
     —— 本地 `origin/main` 跟踪引用陈旧，**不能据此判断推送失败**。
     查真实远端用 `curl -s https://api.github.com/repos/TS-dinglilu/TS-dinglilu.github.io/commits/main`（api 域通常可达）。
  2. **Bash PATH 修复要连带 PortableGit 的 `cmd` 目录**：只 `export PATH="/usr/bin:/bin:$PATH"` 会让
     `publish_all.py` 里的 `subprocess.run(["git", ...])` 抛 `WinError 2`（构建全成功、提交推送直接崩）。
     完整前缀：`/usr/bin:/bin:/c/Users/dingliu/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd`。
  3. **凭据直连通道要重试足 3–4 轮、间隔 ≥25 秒**：本轮第 1、2 次 "Connection was reset"，第 3 次成功；
     常规 `git push` 三次全败（github.com:443 不可达）。
  4. 校验可加一道**更严的自查**（正则扫 doc 标签 / Markdown 链接 / giscus / 信息来源 / inline style / 8000 字符下限），
     比 `publish_all.py` 自带的 3000 字符下限严格。

## 2026-09-15（周二）
- **执行日 09-15**：前置体检通过（13 分类 / 193 报告 / 无漏期），`git pull` 已最新（`7b321f4`）。
- **正文**：5 个并行子代理分工（2+3+2+3+3）写 13 分类，**13/13 一次成功**，无需重派、无 429。
- **新增「外链真实性核验」环节**：206 条唯一外链全量 curl，204 条 200/302；
  2 条修正为 `http://`（`m.yingjiesheng.com` 搜索页、`www.nssc.cas.cn` —— 二者 https 不可达但 http 200）；arXiv 抽验全 200。
- **构建**：`publish_all.py` 一次通过，13/13 生成、索引与主页同步，总数 193 → **206+**，站点自查通过。
- **推送**：提交 `b61b51f`，凭据直连通道**一次成功**；远端 main 已核验为 `b61b51f`，
  线上 13 个 09-15 页面 curl 全 200，主页 13 张卡片均「最新 09-15 · 累计 N 期」。
- **本轮踩坑（已写入项目 MEMORY.md）**：
  1. **Bash 调 `powershell.exe` 被安全策略直接拒绝** → 手动导凭据走不通，用 `publish_all.py --push`；
  2. **`publish_all.py --push` 输出管到 `tail` 会因 SIGTERM 丢掉全部输出** → 必须重定向到日志文件；
  3. **Windows CRLF 临时文件让 shell 循环里的 curl 全返回 `000`** → `tr -d '\r'` 后再读，
     判据是「单条 200、循环全 000 就查数据不查网络」。

## 2026-09-18（周五）
- **开工状态异常（重要）**：`git pull` 后远端 main 仍是 09-15 的 `9f91e8d`；
  但工作区里躺着**整轮 09-17 的未提交成果**（09-17 正文 + 13 份已构建报告 + 索引 + 主页）。
  即 09-17 那轮跑到构建就断了，**从未推送**。另 **09-16 整日无任何分类报告**。
- **处置**：① 不重写 09-17 正文，让它随 09-18 的提交被 `git add -A` 一并带上（已上线验证 200）；
  ② 5 个并行子代理（2+2+3+3+3）写 09-18 正文，**13/13 一次成功**，无 429、无重派。
- **发布**：`publish_all.py --push` 一次通过，凭据直连通道**首推成功**；提交 `dbc06fa`（57 文件），
  远端 main 已核验 = `dbc06fa`；线上 **09-18 页面 13/13 = 200**、主页 13 张卡片「最新 09-18」、总数 **232+**。
- **本轮新增修复（已同步进 DAILY_WORKFLOW.md）**：
  1. **`audit_site.py` 漏期检测的结构性盲区**：原来只做跨分类比对，**某天 13 个分类全缺就查不出来**
     （09-16 因此被静默漏掉）。已加连续区间扫描，全天缺失打 `[WARN] 漏期`。
  2. **新增 `scripts/check_links_today.py`**：外链核验一条命令化（216 条 → 213 有效，`--fix` 改写 7 条为 http）。
     **并发 16 路会误报**（arXiv 大面积超时 000，单条复测全 200）→ 固定 6 路。
     `www.nowcoder.com` 整站对本机不可达，用 WebFetch 核验页面真实存在 → 属站点级屏蔽，不是死链。
  3. **抓出并改写一条编造内容**：`ahut-campus` 的「商学院论坛第41期／黄金升」为不实内容、
     链接 404；实查该院「论坛讲座」栏目最新只到**第（四十）期**，已改写为真实内容 + 真实链接。
     → 教训：`xxx.edu.cn/info/<栏目>/<文章>.htm` 深链 404 = 编号是编的，回栏目列表页找真号。
  4. **Pages 部署延迟**：新文件可能比同批晚 1–3 分钟生效（首查 404、约 2 分钟后 200），
     此时 `contents` API 已能查到文件 → **不要据此重复推送**。
- **遗留**：**09-16 未补做**（13 分类全缺）。判断为不追溯补写：在 09-18 检索结果里回填 09-16 的「新闻」
  极易张冠李戴、与「严禁编造」红线冲突，且会与 09-17/09-18 两期高度重复。
  已通过体检 `[WARN]` 显式暴露，待恒辰决定（补做路径见 DAILY_WORKFLOW.md）。

### 同日第二段（同一个 09-18，另一会话并发执行 → 重复轮次）
> ⚠️ **本轮最大问题：同一自动化在同一天被两个会话并行执行。**
> 上半段（提交 `dbc06fa` 12:10 / `6bf6e92` 12:13）与本段（提交 `40df931` 14:09）
> 各自写了一套 `content/*.html`、各自构建，**互为覆盖**，共产生 3 个 09-18 提交。
> 最终线上以本段 `40df931` 为准（09-18 的 13 份报告由本段正文重建）。
> **建议：跑之前先看 `git log` 最新提交时间与 `logs/push_YYYYMMDD*.log` 是否已存在当天记录，
> 存在就说明当天已被跑过，不要重复生成**（机器/应用并发或跨天恢复时尤其容易撞车）。

- 本段实际交付：**09-17 + 09-18 双日各 13/13**，线上 curl 全 200；主页 13 张卡片「最新 09-18」、总数 **232+**；
  `audit_site.py` 结果 = **0 错误 / 1 警告（09-16 全天缺失）**。
- 正文由 **6 个并行子代理**（2+2+2+2+3+2）产出；主代理撞 429 后全部改用 `model: lite` 立即绕过。
  13/13 经「严格校验」通过：8000–20000 字符、`<div>` 配平、零内联 style、零 Markdown 链接、
  `giscus` 末位、含「信息来源汇总」。
- **外链核验必须带浏览器 UA**（本段关键教训）：不带 UA 时 `news.qq.com` 六条全 501、`cnyouth` 报 404、
  `163/sohu` 报 403；加 `-A 'Mozilla/5.0 …'` 后一律 200。**只有 000 且站点根也 000 才算不可达**
  （`www.gd.gov.cn`、`union.china.com.cn` 属此类；后者经 WebFetch 核实**真实存在**，标题与正文完全对得上）。
- **新抓 1 条伪造外链**：`m.bricksite.com/kjdata/nyhedsbrev?live-blog-…`（丹麦博客平台 + 拼音乱码 slug），
  已删除。**新增判据：host 与内容语种/主题不符 → 直接判伪造**（HTTP 200 也不算数）。
- **正文超长的压缩优先级**（car-recruit 25518 → 19945 字符，链接 50 → 34 仍全部真实）：
  ① 先删与专属分类重复的整块（如 car-recruit 的「新能源与新势力」与 weixiaoli/byd 大幅重叠）；
  ② 再删汇总表里同一来源的冗余行；③ 最后删编辑说明框。**正文条目与链接尽量保住，不靠删硬信息凑数**；
  删完记得重排「板块N」编号。

## 2026-09-19（周六）
- **开工状态**：`git pull` 因 `github.com:443` 不可达失败（正常，跳过）；仓库干净、无 09-19 记录、无并发抢跑。
  体检 13 分类 / 232 报告 / 0 错误 / 1 警告（09-16 全天缺失，既有遗留）。
- **正文产出**：7 个子代理并行；**首轮 5 个代理只交出 7 份**（school-news/ahut-campus/drone-research/
  future-planning/research-institute/weixiaoli-recruit 缺失），补派 2 个代理补齐余下 6 分类。
  ⚠️ **教训（本机复现两次）：子代理的返回文本可能为空、但文件已落盘 —— 别只信回报，必须逐个核对 mtime/大小。**
- **本轮最大问题：篇幅口径**。派写手提示词里写了「文件字节数通常 18K–30K」→ 写手把**字节数当篇幅目标**，
  7/13 分类严重超标（`car-recruit` **43471 字符**、chery 37079、mechanical 35266、geely 33873、
  traditional 29647、byd 29524、xiaomi 24971）。补派 3 个压缩代理（只删不增、保真实链接）压回 15K–19.5K。
  → **已把口径修进文档**：`DAILY_WORKFLOW.md` §2 + FAQ、`content/STYLE_GUIDE.md` §篇幅要求，
  一律写「**12000–19000 字符，用 `len(open(p,encoding='utf-8').read())` 校验**」，严禁再提字节数。
- **新增工具 `scripts/strict_check.py`**（本轮从 `logs/` 移入 `scripts/`）：发布前严格自查
  （8000–20000 字符 / Markdown 链接 / 文档标签 / 内联 style / giscus 末行 / 「信息来源」/ div·a 配平）。
  本轮 13/13 通过。**建议以后每次发布前先跑它。**
- **外链核验**：290 条唯一外链 → 286 有效。4 条人工查证结论：
  CSDN 521 = 限流（WebFetch 实证文章真实、主题对口）→ 留；两个 `job.*.edu.cn` 412 = 整站 WAF 反爬 → 留；
  `campus.nio.com` **https 000、http 只 302 且跳回同样不可达的 https** → 改写为
  `https://www.nio.cn/careers`（200），正文裸文本提及一并替换。
  ⚠️ **新判据：301/302 要跟一次 `-L` 看最终页** —— 落到 000 的跳转不算有效链接。
- **发布**：`publish_all.py --push` 一次通过，**凭据直连通道首推成功**；提交 `8436971`。
- **验证**：远端 main（API）= `8436971` = 本地 HEAD；线上 13 个 09-19 页面全 200
  （`car-recruit` 首轮 404 → 第 2 轮 200，又是 Pages 延迟，未重复推送）；
  主页 13 张卡片「最新 09-19」、统计 **245+**；`audit_site.py` = 0 错误 / 1 警告（09-16）。
- **遗留**：09-16 仍 13 分类全缺，维持不追溯补做的判断。


## 2026-09-25（05:00 触发）— 重复触发改核验
- 开工核查发现 09-25 轮已由凌晨跨天会话完成并推送：`b9f225a`（01:55，23 分类 09-25 报告 + 地区类 09-24/25）+ `f80fede`（02:25，09-24 补账 10 分类），远端 main = `f80fede` = 本地 HEAD。按并发防护规则**未重新生成**。
- 核验：audit_site.py = 0 错误 / 5 警告（09-16、09-20~23 各缺 13 分类，历史欠账待用户拍板）；线上抽查 6 分类 report_20260925 全 200（含地区类与 supply-chain）；主页 23 卡全部「最新 09-25」、累计 291+。
- 收尾：遗留记忆文件小提交 `b863f5e`（本地，随下次发布带上）。
- 无新正文产出，本轮为纯核验轮。

## 2026-09-26（05:00 触发）
- 开工检查：无并发抢跑（无 09-26 记录/日志），git pull 成功，audit 0 错误 0 警告。
- 正文 23/23 一次成功（8 个并行子代理全用 model lite，零 429 零重派）；strict_check 0 不合格。
- 外链 369 条：362 有效，4 条 --fix 改 http，其余 WAF/整站屏蔽的逐条经 WebFetch 或搜索索引验真保留（含两条深链精确搜到原文）。
- 发布：`publish_all.py --push` 凭据直连首推成功，提交 `0694a61`；远端 main = 本地 HEAD。
- 验证：线上 23/23 页面 200（首轮 404 为 Pages 延迟，90 秒后复测转 200）；主页总数 314+；audit 最终全过。
- 本轮全绿，无踩坑、无遗留。
