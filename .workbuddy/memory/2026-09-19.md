# 2026-09-19（周六）工作日志

## 每日自动化新闻推送 · 09-19 一期
- **开工状态**：`git pull` 因 `github.com:443` 不可达失败（预期内，跳过，本地数据完整）；
  仓库干净、无 09-19 记录、无并发会话抢跑。`audit_site.py` 体检：13 分类 / 232 报告 / 0 错误 / 1 警告（09-16 全天缺失，既有遗留，未补）。
- **正文**：7 个子代理并行产出 13 分类（首轮 5 个代理只写出 7 份，补派 2 个代理写出余下 6 分类）。
- **本轮最大问题：篇幅口径搞错**。派写手的提示词写了「文件字节数通常 18K–30K」，
  写手把它当篇幅目标 → 7/13 分类产出 **2.5 万～4.3 万字符**（`car-recruit` 43471），
  远超规范 20000 上限。补派 3 个压缩代理（只删不增、保链接）压回 15K–19.5K。
  → **教训已写入 `DAILY_WORKFLOW.md` §2 与 `content/STYLE_GUIDE.md`**：
  篇幅口径一律写「12000–19000 **字符**，用 `len()` 校验」，并新增 `scripts/strict_check.py`。
- **新增工具**：`scripts/strict_check.py` —— 比 `publish_all.py` 自带校验严格得多
  （8000–20000 字符区间 / Markdown 链接 / 文档标签 / 内联 style / giscus 是否末行 /
  「信息来源」/ div 与 a 标签配平），本轮 13/13 通过（不合格数 0）。
- **外链核验**：`check_links_today.py --fix`，290 条唯一外链 → 286 条有效。4 条需人工确认，逐条查证后结论：
  - `blog.csdn.net/...`（521）：CSDN 限流，域名根 200、文章间歇 200；
    **WebFetch 实证文章真实**（《Simulink四旋翼QAV建模实战》，2026-09-05 发布，主题高度对口）→ 保留。
  - `job.ahu.edu.cn` / `job.cqupt.edu.cn`（412）：**整站 WAF 反爬**（站点根也 412），非死链 → 保留。
  - `campus.nio.com`：https 000、http 仅 302 且跳回同样不可达的 https —— **改写为
    `https://www.nio.cn/careers`**（官方招聘页，200），并把正文里的裸文本提及一并替换。
- **发布**：`publish_all.py --push` 一次通过，13/13 构建 + 索引 + 主页（245+）+ 站点自查通过；
  **凭据直连通道首推成功**，提交 `8436971`（第 2 行是 09-18 的 `01afa87`）。
- **验证**：远端 main（API）= `8436971` = 本地 HEAD；线上 13 个 09-19 页面 curl 全 200
  （`car-recruit` 首轮 404、第 2 轮 200 —— 又一次 Pages 部署延迟，未重复推送）；
  主页 13 张卡片均「最新 09-19 · 累计 N 期」、统计 13 / 245+ / 05:00 / 100%。
- **遗留**：09-16 仍为 13 分类全缺（`[WARN]` 已显式暴露），未补做 —— 保持不追溯的判断。
