# 2026-09-24

## 新增「车企供应链招聘日报」分类（supply-chain-recruit）

恒辰要求在每日自动化新闻系统的招聘选项里加一个车企供应链招聘，格式仿照其它招聘日报。已完成并上线。

### 做了什么
1. **分类注册（7 个脚本）**：tail_template.py 的 `CATEGORY_NAMES`（"车企供应链招聘日报"）、
   publish_all.py / update_homepage.py / audit_site.py / strict_check.py / fix_site.py 的 CATEGORIES 各加一行。
2. **audit_site.py 新增 `START_DATES` 机制**：`{"supply-chain-recruit": "20260924"}`，
   `check_dates` 里漏期比对改为 `d not in cover[c] and d >= START_DATES.get(c, "00000000")`——
   否则新分类会被判「自 20260901 起每天都缺」，一次刷出几十条假 ERROR。
   全天缺失的 WARN 文案也改成动态分类数（原来硬编码"13 个"）。
3. **首期报告**：`content/supply-chain-recruit.html`（19723 字符，8 板块，39 条真实外链，
   strict_check OK）。素材来自当天搜索：宁德时代（供应链与运营类单列、6000+ Offer、3 次志愿）、
   奇瑞集团八大类含采购/供应链/质量类 + iCAR 芜湖硕士采购岗 8千-1.3万×13薪、
   博世中国 9/3 开闸 + 博世力士乐「MATLAB/Simulink 优先」、麦格纳中国 + 动力总成(江西) CAE/NVH、
   均胜电子、三花智控（采购工程师 5 人）、延锋（700 人、供应链管理专业直招、合肥分中心）、
   德赛西威（制造供应链 100 人）、立讯精密、亿纬锂能（采购工程师荆门硕士）、国轩高科（合肥）、
   赛力斯（供应链类独立成类）、东风奕派、一汽物流管理专员、北汽海外供应链岗、SQE 社招行情。
4. **借壳引导脚本** `logs/bootstrap_supply_chain.py`：build_report.pick_template() 只在本分类找模板，
   新分类目录为空 → 借 traditional-auto/report_20260919.html 的壳改名生成
   `supply-chain-recruit/report_20260924.html` + 复制归档页 index.html 后 rebuild_index()。
   ⚠️ **借壳只取到 hero 为止**（`tpl[:tpl.find('<div class="report-header">')]`）：
   build_report 的 head 切片取「首个内容锚点之前」，会把模板历史累积的
   report-header/highlight-box 连同「传统车企」正文块一起带过来，必须截断。
5. **主页**：index.html auto-grid 在 traditional-auto 卡后插入新卡片（data-cat="recruit"、🚚、
   橙色 icon、animation-delay:0.58s），底部导航加「车企供应链」链接；update_homepage.py 刷出
   「最新 09-24 · 累计 1 期」，总数 246+。
6. **文档**：DAILY_WORKFLOW.md（13→14 分类、新增 §0.3「新增一个日报分类」六处注册清单、
   FAQ 加「新增分类后刷出几十条漏期」条目）、README.md 同步 14 分类 + 新增章节。
   每日自动化任务改名「每日自动化新闻推送-14分类日报」并在 prompt 里补上新分类。
7. **发布**：`publish_all.py --categories supply-chain-recruit --no-build --push`
   （首期报告是借壳生成的，pick_template 会因目录里只有目标文件而拒绝重建，故 --no-build）。
   推送走凭据直连超时 → 常规 push 第 2 次成功。远端提交 44f5d54（2026-09-24 19:54 本地），
   线上 report/archive 200、主页卡片引用 2 处 ✓。

### 站点现状（要告诉恒辰的）
- 自动化自 09-19 之后就没跑过：**09-20~09-24 共 5 天 13 个老分类全缺**（audit 有 WARN），
  09-24 因新分类有报告而报 1 条 ERROR「缺 13 个分类」。要么用 build_backfill 补，
  要么等明天 05:00 自动化恢复（只会补 09-25）。
- content/byd-recruit.html（26055 字符）、content/weixiaoli-recruit.html（34249 字符）是
  09-20 中断会话留下的未提交草稿，strict_check 报 LEN 超标，且未构建——不要重写，
  压缩后再构建，或直接当 09-24 素材改写。

## 20:46 归档页报告卡片改新选项卡
- 恒辰反馈: 主页→归档页是新选项卡, 但归档页里点报告会覆盖归档页。
- 修法: `build_report.py` `rebuild_index()` 的 report-card `<a>` 加 `target="_blank"`（全站 14 分类统一生效, 与主页行为一致）+ 重建 `supply-chain-recruit/index.html`。
- 提交 `22ff170`, push 一次成功, 线上已验证 200 且 target=_blank 生效。

## 22:32 手机端/电脑端适配检查
- 用 Edge headless + playwright-core 写了响应式检测脚本 `logs/responsive_check.js`：375/1440 两档宽度, 检测 scrollWidth 溢出 + 截图, 覆盖主页/供应链归档页/供应链报告页。
- 结论: 6 项全部无横向溢出, 模板自带 600/768 断点够用, 无需大改 CSS。
- 顺手修了 3 个真问题(提交 ee14885, 凭据直连通道推送):
  1. 主页 favicon data URL 被编辑器注入 data-page-node-id, 双引号截断 href 导致左上角漏出 '">' 文字+图标挂 — 已还原干净 favicon(仅 index.html 中招);
  2. 主页统计 13->14(简介/自动化系统/SYSTEMS LIVE 三处);
  3. 供应链归档页 og:url 还指 traditional-auto, 已修正。
- update_homepage.py 只更新「已生成日报 N+」, 不碰「自动化系统」, 14 不会被回写; 其文档注释 13->14。
- 教训: favicon 这类 SVG data URL 里不能再被注入双引号属性; 以后主页异常先查 data-page-node-id 注入点。

## 23:25 优化整改
- audit 体检: 1 ERROR(0924缺13老分类) + 5 WARN(0916/0920/0921/0922/0923 全天缺失; 0916 也是漏的, 比此前记忆多一天)。
- 清理: git rm 根目录 16 个 stats_*.txt(被 git 跟踪的临时统计输出), 删 scripts/__pycache__ pyc, .gitignore 加 /stats_*.txt。
- 重要勘误: content/byd、weixiaoli 两份草稿**不是 09-20 的, 是 2026-09-24 的**(头部日期明确), 此前「09-20 中断会话遗留」记载有误。
- 两轮+微调三轮裁剪(日志 logs/trim_0924_drafts*.py): byd 30579->19812字52链接, weixiaoli 37142->19925字51链接, strict_check 全绿。
  删块原则执行: byd 删板块二(销量出海,与car重叠)/板块四(具身智能,与car/drone重叠)+12行汇总; weixiaoli 删板块五岗位卡(与板块三四内部重复)/低空warning-box(与板块六风险内部重复)+6条目(就业大盘/NIO补能/乐道/小鹏基本面/理想i9/行业换轨)+15行汇总。
- 构建 byd/weixiaoli 0924 两期并发布, 主页 248+。0924 缺口 13->11。
- push 波折: 首推 send-pack 断连且误报 up-to-date(API 验证才发现没推上), 第 4 次凭据通道才成功 -> **push 后必须用 API 验 SHA, 不能信 git 输出**。
- 待办: 0916+0920x11+0921~0923x13 = 50 份补期(需真实素材, 大工程, 等恒辰定策略); 0924 还缺 11 个分类(明早 05:00 自动化只会做 0925)。
